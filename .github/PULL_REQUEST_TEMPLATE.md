## What does this PR do?

<!-- New word set? level tweak? CLI change? -->

## Checklist

- [ ] Tests pass (`pytest -q`), including `test_facts_preserved_*` and `test_negation_never_dropped`
- [ ] `python -m compileall caveman` is clean
- [ ] Core stays dependency-free (tiktoken remains an optional extra)
